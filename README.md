# GenAI-Course
Generative AI (gen AI) is a category of artificial intelligence that can create new content — text, images, audio, video, code, and more — rather than just analyzing or classifying existing data.  It works by learning patterns from massive amounts of training data, then using those patterns to generate new outputs that resemble what it learned.
